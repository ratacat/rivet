# ABOUTME: Canonical terminology for the project
# ABOUTME: Each entry is a system that deserves disambiguation

terms: {}

# Add entries using the CLI (once built) or manually following schema-draft.yaml

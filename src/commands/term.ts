// ABOUTME: rivet term <system> <term> - lock a term to a SPECIFIC SYSTEM
// ABOUTME: Code names, function names, class names belonging to that system

// Usage:
//   rivet term Router createRouter
//   rivet term Config loadConfig

// DIFFERENCE FROM 'rivet define':
//   - 'term' = code names locked to a specific system (Router.createRouter)
//   - 'define' = project-wide concepts, jargon, conventions (glossary)

// Terms are locked once set. To rename, use rivet purge.

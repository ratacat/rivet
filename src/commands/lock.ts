// ABOUTME: rivet lock <system> <identifier> - lock a code identifier to a system
// ABOUTME: Function names, class names, variable names belonging to that system

// Usage:
//   rivet lock Router createRouter
//   rivet lock Config loadConfig

// DIFFERENCE FROM 'rivet term':
//   - 'lock' = code identifiers locked to a specific system (Router.createRouter)
//   - 'term' = project-wide glossary (concepts, jargon, conventions)

// Locked identifiers should not drift. To rename, use rivet purge.
